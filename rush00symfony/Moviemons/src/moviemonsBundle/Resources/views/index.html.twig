<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <style type="text/css">
            ul {
                padding-top : 70px;
            }
            li {
                list-style-type: none;
                font-size: 24px;
                vertical-align: middle;
            }
            body {
                text-align: center;
            }
            table, td, tr {
                border: 1px solid black;
            }
            td, tr {
                height : 20%;
                width: 15%;
            }
            table {
                width : 100%; border-collapse: collapse; height: 500px; text-align: center;
            }
            a:link {
                text-decoration: none;
            }

            a:visited {
                text-decoration: none;
            }

            a:hover {
                text-decoration: underline;
            }

            a:active {
                text-decoration: underline;
            }
        </style>
    </head>
    <body>
        {% block body %}
            {{ content | raw }}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>